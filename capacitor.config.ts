import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'qrcode_scanner',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
